/**
 * Exact release timestamps override the recurring country/indicator rules.
 *
 * Keep entries sorted and use ISO-8601 timestamps. This file is intentionally
 * versioned so future official release calendars can be reviewed in Git.
 */
export const releaseCalendar: Record<string, string[]> = {};
